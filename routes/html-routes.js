let express = require("express");
let router = express.Router();

/* router.get("/", (req, res) =>
  db.Book
    .findAll()
    .then((books) => {
      console.log(books);
      res.render("index", {
        Book: books,
      });
    })
    .catch((err) => console.log(err))
 );*/



module.exports = router;
